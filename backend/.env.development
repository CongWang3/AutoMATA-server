# =============================================================================
# 本地开发 — 默认 APP_ENV 未设时由 Settings 加载本文件（及 .env.development.local）
# =============================================================================
# 机密/个人覆盖：使用 .env.development.local（gitignore，勿提交）

DEBUG=True
APP_NAME=AutoMATA
APP_VERSION=1.0.0
API_PREFIX=/api/v1
HOST=0.0.0.0
PORT=8005

DB_USER=automata
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=3306
DB_NAME=automata

SECRET_KEY=4Fmbn2CcRtpWyjgeDUAW5nQIZ0lrMhDqF7AQO_DEu7Y
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

MAX_UPLOAD_SIZE=524288000
UPLOAD_DIR=./uploaded_files

REDIS_URL=redis://localhost:6379/0

# DEBUG=True 且为默认本机 8001 时，download_public_base 会置空，便于 Vite 反代
DOWNLOAD_PUBLIC_BASE_URL=http://localhost:8001

# 本地前端 Vite
CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:3000","http://127.0.0.1:5173"]

EMAIL_ENABLED=false
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SSL=false
SMTP_FROM_NAME=AutoMATA

AGENT_ENABLED=false
AGENT_DEFAULT_PROVIDER=openai
