# =============================================================================
# 说明文档 — Vite 不读此文件；后端由 config/settings.py 按 APP_ENV 选择 dotenv
# =============================================================================
#
# 已提交默认：
#   .env.development   → 本地开发（APP_ENV 未设或 development）
#   .env.production    → 仅当 APP_ENV=production 时参与加载（模板，无真实密钥）
#
# 个人/机密（gitignore）：
#   .env.development.local
#   .env.production.local
#
# 兼容：若上述主文件不存在，会回退加载 legacy 单文件 .env
#
# Docker 部署：deploy/.env.prod 经 compose 注入环境变量；镜像内可不包含任何 .env。
# 请在 .env.prod 中设置 APP_ENV=production（与 compose environment 一致）。
#
# ---------------------------------------------------------------------------
# 关键变量
# ---------------------------------------------------------------------------
# APP_ENV / ENVIRONMENT     development | production | prod（仅来自 shell/Compose，勿指望写在第一个 dotenv 里决定加载谁）
# DEBUG                     开发 True，生产 False
# DB_* / REDIS_URL          容器内用服务名 mysql、redis
# CORS_ORIGINS              JSON 数组字符串；生产勿用 *
# DOWNLOAD_PUBLIC_BASE_URL  与主站同域反代时可留空
# SECRET_KEY                  生产强随机
