# =============================================================================
# automata-web 环境变量说明（本文件不参与 Vite 加载，仅作文档）
# =============================================================================
#
# 已纳入版本库的两套默认配置：
#   .env.development  → npm run dev
#   .env.production     → npm run build / Docker 构建（部署按生产）
#
# 个人或机密覆盖（勿提交）：
#   .env.development.local   覆盖开发
#   .env.production.local    覆盖本地生产构建
#
# ---------------------------------------------------------------------------
# 变量说明
# ---------------------------------------------------------------------------
# VITE_API_BASE_URL
#   开发默认 /api（走 Vite 代理）；生产默认 /api（走站点网关）。
#   直连后端时设完整 URL，且通常配合 VITE_DIRECT_API=true。
#
# VITE_DIRECT_API
#   true 时浏览器直连 VITE_API_BASE_URL，不走同源代理（仅开发常用）。
#
# VITE_PUBLIC_SITE_ORIGIN
#   可选。不设则用 window.location.origin。
#   生产 Docker：在 GitHub Repository Variables 配置，由 workflow build-args 传入。
#
# VITE_ENCRYPTION_KEY
#   前端 secureStorage 用；须与部署环境一致，勿在公共示例中填真实生产密钥。
#
# VITE_WS_BASE_URL / VITE_DEV_WS_ORIGIN / VITE_DEV_API_ORIGIN
#   见 src/config/deploy.ts（直连或代理模式）。
#
# ---------------------------------------------------------------------------
# 开发环境 — 复制到 .env.development.local 可按需改
# ---------------------------------------------------------------------------
# VITE_DIRECT_API=false
# VITE_API_BASE_URL=/api
# VITE_LOGO_PATH=/images/logo.png
# VITE_APP_TITLE=AutoMATA Bioinformatics Platform
# VITE_ENCRYPTION_KEY=your-dev-key

# ---------------------------------------------------------------------------
# 生产环境 — 与 .env.production 对齐；CI 额外注入 VITE_PUBLIC_SITE_ORIGIN
# ---------------------------------------------------------------------------
# VITE_API_BASE_URL=/api
# VITE_LOGO_PATH=/images/logo.png
# VITE_APP_TITLE=AutoMATA Bioinformatics Platform
# VITE_ENCRYPTION_KEY=your-production-key
# VITE_PUBLIC_SITE_ORIGIN=https://your-public-site.example.com
